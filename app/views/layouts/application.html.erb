<!DOCTYPE html>
<html>
  <head>
    <title>InstagramClone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0" crossorigin="anonymous">
  </head>

  <body>
  <!-- Image and text -->

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"><%= image_tag "nekostagram.png", width: 200, class: "d-inline-block align-top" %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
      <% if logged_in? %>
      <%= link_to "Profile", user_path(current_user.id), class:"header-menu" %>
      <%= link_to "Logout", session_path(current_user.id), method: :delete, class:"header-menu" %>
    <% else %>
    　<%= link_to "Sign up", new_user_path, class:"header-menu" %>
    <%= link_to "Login", new_session_path, class:"header-menu" %>
    <% end %>
    </ul>
  </div>
</nav>

<nav class="navbar fixed-bottom navbar-light bg-light">
  <p class="navbar-brand" id= "footer">© 2021 NEKOSTAGRAM DEVELOPED BY MACHIKO</p>
</nav>

    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>
    <%= yield %>
  </body>
</html>
